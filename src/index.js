import React from "react";
import ReactDOM from "react-dom";
import App from "./App";
// Remove or add the import if needed
// import './index.css';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);

// Remove or add the import and call if needed
// import reportWebVitals from './reportWebVitals';
// reportWebVitals(console.log);
